#include <iostream>
#include <string_view>
#include <deque>
#include <string>
using namespace std;

bool search(string name){
    deque<string> search_queue;
    search_queue.push_back(graph[name]);
    

} 

int main(){
  
    return 0;
}